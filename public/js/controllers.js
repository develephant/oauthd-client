
angular.module('app.controllers', [])

.controller("DashboardController", ['$scope',
function( $scope ) {

}])

.controller('MainController', ['$scope',
function( $scope ) {
    $scope.title = "Happy";
}]);
